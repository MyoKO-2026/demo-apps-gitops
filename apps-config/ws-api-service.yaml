apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-golang
  namespace: demo-app
  labels:
    app: api-golang
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-golang
  template:
    metadata:
      labels:
        app: api-golang
    spec:
      containers:
        - name: test-service
          image: replace-image
          env:
            - name: PORT
              value: "8000"
          envFrom:
            - secretRef:
                name: api-golang-database-url
          ports:
            - containerPort: 8000
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /ping
              port: 8000
          resources:
            limits:
              memory: "100Mi"
            requests:
              memory: "100Mi"
              cpu: "50m"
          securityContext:
            allowPrivilegeEscalation: false
            privileged: false
      securityContext:
        seccompProfile:
          type: RuntimeDefault

---
apiVersion: v1
kind: Service
metadata:
  name: api-golang
  namespace: demo-app
spec:
  selector:
    app: api-golang
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000

---

